// Stub implementation - client-side NFT parsing disabled for now
export async function parseMeksFromUtxos(
  utxos: any[],
  stakeAddress: string,
  existingMeks: any[]
) {
  console.log('[nftExtractor] Stub - client-side parsing disabled');
  return [];
}
